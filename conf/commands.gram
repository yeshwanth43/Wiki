#JSGF V1.0;

/**
 * JSGF Grammar for Hello World example
 */

grammar commands;

public <command> = ( shutdown application) | ( Skip Introduction ) | ( start wiki ) | ( start Application ) | ( switch to home ) | ( back to wiki ) | ( start synthesizer ) | ( start readability calculations ) | ( Parts of speech enhancement  ) | (  About us ) | (  About Developers );
